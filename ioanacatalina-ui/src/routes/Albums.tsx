import { Typography } from 'UI/Typography/Typography';

export default function Albums() {
    return (
        <div>
            <Typography.H2 alignCenter>Albums</Typography.H2>
        </div>
    );
}
